# Mailgun Configuration
# Set these using: firebase functions:config:set mailgun.key="xxx" mailgun.domain="xxx"
# Or create a .env file (gitignored) for local development

MAILGUN_API_KEY=your_mailgun_api_key
MAILGUN_DOMAIN=your_domain.com

# Firebase Admin SDK
# For local development, set GOOGLE_APPLICATION_CREDENTIALS env var to point to your service account JSON
# For production, Firebase Functions automatically handles this
